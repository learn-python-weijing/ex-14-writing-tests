# Exercise 14 — YOUR tests go here!
# The functions in solution.py are already correct.
# Write at least 3 tests per function.
# Run: pytest

from solution import clamp, title_case, is_anagram, chunk, running_total

# --- Write your tests below ---

# Example structure:
# def test_clamp_returns_value_within_range():
#     assert clamp(5, 0, 10) == 5
